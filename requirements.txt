# SpliceVarMech — Requirements
# Install with: pip install -r requirements.txt

# ── Phase 1 & 2: Data parsing, feature extraction ──
openpyxl>=3.1.0
pandas>=2.0.0
numpy>=1.24.0

# ── Phase 3: Bayesian causal inference & diagnostics ──
pymc>=5.0.0
arviz>=0.15.0
scipy>=1.10.0

# ── Phase 4: Diffusion model (deep learning) ──
torch>=2.0.0
einops>=0.7.0

# ── Evaluation & baselines ──
scikit-learn>=1.3.0

# ── Testing ──
pytest>=7.0.0
